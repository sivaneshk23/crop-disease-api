services:
  - type: web
    name: crop-disease-api
    env: python
    buildCommand: pip install -r requirements.txt && curl -L -o model.pth https://github.com/sivaneshk23/crop-disease-api/releases/download/v1.0.0/model.pth
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
